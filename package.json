{
    "name": "lfw-app",
    "version": "1.0.0",
    "engines": {
        "node": "20.x"
    },
    "scripts": {
        "start": "node back/server.js",
        "heroku-postbuild": "cd front && npm install && npm run build"
    }
}
