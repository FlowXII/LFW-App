{
    "name": "lfw-app",
    "version": "1.0.0",
    "engines": {
        "node": "20.x"
    },
    "scripts": {
        "start": "node back/server.js",
        "heroku-postbuild": "npm install --prefix back && npm install --prefix front && npm run build --prefix front"
    }
}
